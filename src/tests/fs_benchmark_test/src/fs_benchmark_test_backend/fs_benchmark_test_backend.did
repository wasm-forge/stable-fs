service : {
    "greet": (text) -> (text) query;

    "write_mib_text": (text, nat64) -> (nat64);

    "write_kib_text": (text, nat64) -> (nat64);

    "write_text": (text, text, nat64) -> (nat64);

    "read_text": (text, int64, nat64) -> (text);
}
